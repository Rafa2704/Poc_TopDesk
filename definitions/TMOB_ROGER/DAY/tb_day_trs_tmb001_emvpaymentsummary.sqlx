config {
    type: "table",
    schema: "day",
    name: "tb_day_trs_tmb001_emvpaymentsummary",
    tags: ["tmb005"],
    description: "Lista de assets ATM do TOPdesk"
}

SELECT
    id,
    CAST(date AS DATE) AS date,
    CAST(month AS DATE) AS month,
    emvAcquirerCode,
    emvProductId,
    emvBinBin,
    brand,
    emvAidAid,
    approvedCount,
    approvedAmount,
    denyCount,
    denyAmount,
    totalCount,
    totalAmount,
    blameUser,
    CAST(createdAt AS TIMESTAMP) AS createdAt,
    CAST(updatedAt AS TIMESTAMP) AS updatedAt,
    debtAmount,
    debtCount,
    acquirerDenyAmount,
    acquirerDenyCount,
    toTransportOperatorId,
    mode,
    "TMB001" AS origem,
    CURRENT_TIMESTAMP() AS dt_processamento,
    SESSION_USER() AS usuario_processamento
FROM
    ${ref("tb_ext_trs_tmb001_emvpaymentsummary")}